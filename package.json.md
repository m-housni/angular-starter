### General Information
- **Project Name:** `angular-starter`
- **Version:** `0.0.0`
- **Private:** `true` (not published to npm)

---

### **Scripts**
| Command      | Description                                |
|--------------|--------------------------------------------|
| `ng`         | Alias for Angular CLI.                    |
| `start`      | Starts the development server (`ng serve`).|
| `build`      | Builds the application for production.     |
| `watch`      | Watches for file changes (`--watch`).      |
| `test`       | Runs unit tests with Karma.                |

---

### **Dependencies**
| Package                        | Version         | Purpose                                     |
|--------------------------------|-----------------|---------------------------------------------|
| `@angular/*`                   | `^19.0.0`       | Core Angular libraries for v19.             |
| `rxjs`                         | `~7.8.0`        | Reactive programming support.               |
| `tslib`                        | `^2.3.0`        | TypeScript helper library.                  |
| `zone.js`                      | `~0.15.0`       | Angular's execution context manager.        |

---

### **Dev Dependencies**
| Package                        | Version         | Purpose                                     |
|--------------------------------|-----------------|---------------------------------------------|
| `@angular/cli`                 | `^19.0.5`       | Angular CLI for development tasks.          |
| `@angular-devkit/build-angular`| `^19.0.5`       | Angular build tools.                        |
| `@types/jasmine`               | `~5.1.0`        | Type definitions for Jasmine.               |
| `jasmine-core`                 | `~5.4.0`        | Jasmine testing framework.                  |
| `karma`                        | `~6.4.0`        | Test runner for Angular.                    |
| `typescript`                   | `~5.6.2`        | TypeScript compiler.                        |
| `karma-*` packages             | Various         | Test reporters and launchers for Karma.     |

---

### Key Notes:
1. This project uses **Angular v19**.
2. The **TypeScript** version aligns with Angular requirements.
3. **Karma** and **Jasmine** are set up for testing.
4. CLI tools like `@angular/cli` handle builds, testing, and development tasks.
